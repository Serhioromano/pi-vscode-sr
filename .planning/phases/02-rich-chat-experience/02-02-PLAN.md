---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vscode-ext/src/event-mapper.ts
  - vscode-ext/tests/event-mapper.test.ts
autonomous: true
requirements: [CHAT-03]

must_haves:
  truths:
    - Tool execution renders as collapsible `<details>`/`<summary>` HTML sections in verbose mode (D-02, D-03)
    - Quiet mode suppresses all tool execution details, showing only progress (D-01)
    - `mapAgentEventToAction` accepts optional `toolVisibility` parameter with default `"verbose"` (backward-compatible)
    - Old placeholder text "error executing {toolName}..." is replaced with collapsible tool sections
    - Tests cover verbose HTML output, quiet mode silence, and backward-compatible 1-arg call
  artifacts:
    - path: vscode-ext/src/event-mapper.ts
      provides: ToolVisibility type, tool-buffered mapAgentEventToAction, buildToolSection helper
      exports: ['StreamAction', 'mapAgentEventToAction', 'applyStreamAction', 'streamEvents', 'ToolVisibility']
    - path: vscode-ext/tests/event-mapper.test.ts
      provides: Test coverage for verbose/quiet modes and tool buffering
  key_links:
    - from: event-mapper.ts ToolVisibility
      to: chat-handler.ts
      via: imported by chat-handler.ts for settings typing
    - from: mapAgentEventToAction
      to: ChatResponseStream
      via: applyStreamAction
---

<objective>
Upgrade the event mapper with user-configurable tool visibility (D-01, D-02, D-03). Tool execution events are buffered per-execution and emitted as complete `<details>`/`<summary>` collapsible sections at `tool_execution_end`. The old placeholder "error executing..." text is removed. In quiet mode, all tool output is suppressed. The function signatures remain backward-compatible (optional ToolVisibility parameter defaults to 'verbose').

Purpose: Provide the rendering foundation that chat-handler streaming (Plan 02-03) and settings wiring (Plan 02-04) will consume.
Output: Updated event-mapper.ts with ToolVisibility type, buffered tool rendering, `<details>` HTML; updated tests.
</objective>

<execution_context>
@/home/sergey/www/pi-vscode-sr/.claude/gsd-core/workflows/execute-plan.md
@/home/sergey/www/pi-vscode-sr/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-rich-chat-experience/02-CONTEXT.md (D-01, D-02, D-03)
@.planning/phases/02-rich-chat-experience/02-RESEARCH.md (Code Example 2, Pitfall 3)
@.planning/phases/02-rich-chat-experience/02-UI-SPEC.md (Copywriting Contract, Interaction Contract)
@vscode-ext/src/event-mapper.ts
@vscode-ext/tests/event-mapper.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ToolVisibility type, tool buffering, and collapsible HTML sections to event-mapper.ts</name>
  <files>vscode-ext/src/event-mapper.ts</files>
  <read_first>vscode-ext/src/event-mapper.ts</read_first>
  <action>
    Modify `vscode-ext/src/event-mapper.ts` to support user-configurable tool visibility. Changes are backward-compatible — existing callers using 1-arg `mapAgentEventToAction(event)` continue to work with `toolVisibility` defaulting to `"verbose"`.

    Changes in order of occurrence in the file:

    1. **Export `ToolVisibility` type**: `export type ToolVisibility = 'verbose' | 'quiet';`

    2. **Extend `StreamAction` union type**: Add `MarkdownString` import from `vscode`. Change `value: string` on the `markdown` variant to `value: string | import('vscode').MarkdownString`. This allows emitting HTML-rich markdown for tool sections:
       ```typescript
       | { type: 'markdown'; value: string | MarkdownString }
       ```

    3. **Module-level tool buffer**: Add a module-level variable (not inside any function — same pattern as the existing module-level state):
       ```typescript
       let toolBuffer: { toolName: string; args: string; partialResults: string[]; isError: boolean } | null = null;
       ```
       This buffers events for one tool execution at a time. Per Pitfall 3 (RESEARCH.md), tool sections MUST be emitted as a single complete block.

    4. **Update `mapAgentEventToAction` signature**: Add optional second parameter:
       ```typescript
       export function mapAgentEventToAction(
         event: AgentEvent,
         toolVisibility: ToolVisibility = 'verbose'
       ): StreamAction {
       ```

    5. **Update event handlers**:

       - `agent_start`: No change needed.
       - `turn_start`: No change needed.
       - `message_update`: No change needed.
       - `tool_execution_start` (REPLACE the current placeholder):
         - Quiet mode: return `{ type: 'markdown', value: '' }` — suppress all tool output.
         - Verbose mode: Initialize `toolBuffer` with `{ toolName: event.toolName, args: JSON.stringify(event.args ?? {}), partialResults: [], isError: false }`. Return `{ type: 'progress', value: 'Tool: ' + event.toolName }` — show progress immediately but wait for completion before emitting the collapsible section.
       - `tool_execution_update`:
         - If verbose mode and toolBuffer exists: accumulate `partialResult` if it's a string (`toolBuffer.partialResults.push(result)`). Return `{ type: 'markdown', value: '' }` — no per-update output (all buffered for end).
         - If quiet or no toolBuffer: return `{ type: 'markdown', value: '' }`.
       - `tool_execution_end`:
         - If quiet mode or no toolBuffer: Reset `toolBuffer = null`, return `{ type: 'markdown', value: '' }`.
         - Verbose mode: Set `toolBuffer.isError = event.isError`. Build a complete `<details>` block using `buildToolSection(toolBuffer)`. Reset `toolBuffer = null`. Return `{ type: 'markdown', value: builtMarkdownString }`.
       - `message_end`: Reset `toolBuffer = null` to prevent orphaned buffers across messages. Return unchanged.
       - `agent_end`: Reset `toolBuffer = null`.

    6. **Add `buildToolSection` helper function** (private, not exported):
       ```typescript
       function buildToolSection(buf: {
         toolName: string;
         args: string;
         partialResults: string[];
         isError: boolean;
       }): MarkdownString {
         const status = buf.isError ? '$(error) Failed' : '$(check) Completed';
         const resultContent = buf.partialResults.join('\n') || '(no output)';
         const html = '<details>\n<summary><strong>Tool: '
           + escapeHtml(buf.toolName)
           + '</strong> -- '
           + status
           + '</summary>\n\n```\n'
           + escapeHtml(resultContent)
           + '\n```\n\n</details>';
         const ms = new MarkdownString(html);
         ms.supportHtml = true;   // Critical: enables <details>/<summary> rendering (D-03)
         ms.isTrusted = true;     // Allows command: links if needed later
         return ms;
       }
       ```
       Use `$(error)` (circle slash) and `$(check)` (checkmark) Codicons for visual status indicators per UI-SPEC Copywriting Contract.

    7. **Add `escapeHtml` private function**:
       ```typescript
       function escapeHtml(s: string): string {
         return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
       }
       ```

    8. **Update `applyStreamAction`**: The `markdown` case already handles string values. Add handling for `MarkdownString` values — `stream.markdown()` accepts both `string` and `MarkdownString` in the VS Code API. No changes needed if the existing code passes through to `stream.markdown()`, but verify: the current code `if (action.value) stream.markdown(action.value)` — since `action.value` is now `string | MarkdownString`, TypeScript may warn. Use a type guard or cast: cast the value parameter to `string | vscode.MarkdownString` in the `stream.markdown()` call. Import `MarkdownString` type with `import type { MarkdownString } from 'vscode'`.

    9. **Buffer reset on `message_end` and `agent_end`**: Add `toolBuffer = null` in the `message_end` case (before return) and `agent_end` case (before the `return { type: 'done' }`). This prevents stale tool buffers from leaking across turns.
  </action>
  <verify>
    <automated>cd vscode-ext && npx vitest run tests/event-mapper.test.ts --reporter verbose</automated>
  </verify>
  <acceptance_criteria>
    - `cd vscode-ext && npx tsc --noEmit` passes with no errors
    - `mapAgentEventToAction(event)` (1-arg call) still compiles and defaults to verbose mode
    - `tool_execution_start` in verbose mode returns `{ type: 'progress', value: 'Tool: {name}' }` (not the old error text)
    - `Tool: {name}` — confirm the exact string from UI-SPEC Copy Table line 9
    - `tool_execution_end` in verbose mode returns a `markdown` action where `value` is a `MarkdownString` with `supportHtml = true`
    - `buildToolSection` output contains `<details>` and `<summary>` tags
    - Quiet mode (`toolVisibility='quiet'`): tool_execution_* events return `{ type: 'markdown', value: '' }` (suppressed)
    - Quiet mode: tool_execution_start does NOT return progress (no "Tool: {name}" in quiet mode). Actually revert: per UI-SPEC line 91, quiet mode shows only "Pi is working..." — so tool_execution_start should also return empty markdown.
    - `toolBuffer` is reset to null on `message_end` and `agent_end`
    - All existing tests still pass after changes
  </acceptance_criteria>
  <done>
    event-mapper.ts exports ToolVisibility type. mapAgentEventToAction accepts optional toolVisibility parameter. Tool events buffer per-execution and emit complete `<details>` blocks. Old error placeholder replaced. All existing and new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update event-mapper tests for tool visibility and buffering</name>
  <files>vscode-ext/tests/event-mapper.test.ts</files>
  <read_first>
    vscode-ext/tests/event-mapper.test.ts
    vscode-ext/src/event-mapper.ts
  </read_first>
  <action>
    Extend the existing test file `vscode-ext/tests/event-mapper.test.ts` with new test coverage for Phase 2 changes. Add tests inside the existing `describe('mapAgentEventToAction', ...)` block and add a new `describe('applyStreamAction with MarkdownString', ...)` block.

    New tests to add:

    **Inside `describe('mapAgentEventToAction')`:**

    1. `'tool_execution_start returns progress in verbose mode'`:
       - event: `{ type: 'tool_execution_start', toolName: 'read', args: { file: 'test.ts' } }` as AgentEvent
       - `mapAgentEventToAction(event)` returns `{ type: 'progress', value: 'Tool: read' }`

    2. `'tool_execution_start returns empty markdown in quiet mode'`:
       - Same event, called with `mapAgentEventToAction(event, 'quiet')`
       - Expect `{ type: 'markdown', value: '' }`

    3. `'tool_execution_start without toolVisibility defaults to verbose'`:
       - Call with 1 argument: `mapAgentEventToAction(event)`
       - Expect progress action — confirms backward compatibility

    4. `'tool_execution_update accumulates partial results in verbose mode'`:
       - Call `tool_execution_start` first to set up buffer (but buffer is module-level, test may need to call start then update in sequence)
       - Note: since toolBuffer is module-level, sequential calls in the same test matter. Test by calling mapAgentEventToAction for tool_execution_start first, then tool_execution_update — verify that the update returns `{ type: 'markdown', value: '' }` (no per-update output).

    5. `'tool_execution_update returns empty in quiet mode'`:
       - event: `{ type: 'tool_execution_update', toolName: 'read', partialResult: 'content' }`
       - `mapAgentEventToAction(event, 'quiet')` returns `{ type: 'markdown', value: '' }`

    6. `'tool_execution_end returns collapsible section in verbose mode'`:
       - Call start, update, then end in sequence
       - Verify the returned action `type` is `'markdown'` and `value.supportHtml` is `true`
       - Verify the MarkdownString value contains `<details>` and `<summary>`

    7. `'tool_execution_end returns empty in quiet mode'`:
       - Call with quiet mode, expect `{ type: 'markdown', value: '' }`

    8. `'tool_execution_end with isError shows failed status'`:
       - Call with tool_execution_start, then tool_execution_end with `isError: true`
       - Verify the MarkdownString value contains `Failed` or `$(error)`

    9. `'tool_execution_end with isError false shows completed status'`:
       - Same, with `isError: false`, verify contains `Completed` or `$(check)`

    10. `'message_end resets tool buffer'`:
        - Call tool_execution_start first, then message_end. Followed by tool_execution_end should not emit a section (buffer already reset).

    **New `describe('applyStreamAction with MarkdownString')`:**

    1. `'calls stream.markdown with MarkdownString value'`:
       - Create a `MarkdownString` with `<details>test</details>` and `supportHtml = true`
       - Call `applyStreamAction(stream, { type: 'markdown', value: md })`
       - Verify `stream.markdown` was called with the MarkdownString

    Use the same test patterns as the existing tests (mock stream objects). Add `import { MarkdownString } from 'vscode'` at the top — but in a unit test environment, `vscode` may not be available. Instead, construct the string directly: `const md = { value: '<details>...</details>', supportHtml: true, isTrusted: true }`. Use inline type matching rather than MarkdownString class construction.

    Note: The module-level `toolBuffer` variable creates ordering dependence between tests. Since vitest runs tests in isolation per file, each describe block sees a fresh module state. But within a single `it()` block, call order matters. Document with comments that tool execution tests must call start-update-end in sequence.
  </action>
  <verify>
    <automated>cd vscode-ext && npx vitest run tests/event-mapper.test.ts --reporter verbose</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/event-mapper.test.ts` passes with all tests green
    - Verbose mode tests: tool_execution_start returns progress, tool_execution_update returns empty, tool_execution_end returns MarkdownString with `<details>`
    - Quiet mode tests: all tool events return `{ type: 'markdown', value: '' }`
    - Backward compatibility: 1-arg call defaults to verbose progress
    - Buffer reset: message_end clears buffer so subsequent tool_execution_end produces no output
    - applyStreamAction tests verify MarkdownString is passed to stream.markdown
  </acceptance_criteria>
  <done>
    All new and existing event-mapper tests pass. Tool visibility and buffering behavior is fully tested in both verbose and quiet modes.
  </done>
</task>

</tasks>

## Artifacts This Phase Produces

### New symbols (functions, classes, types, interfaces)

| Symbol | Kind | Location |
|--------|------|----------|
| `ToolVisibility` | type alias (`'verbose' \| 'quiet'`) | event-mapper.ts |
| `toolBuffer` | module-level variable | event-mapper.ts |
| `buildToolSection` | function (private) | event-mapper.ts |
| `escapeHtml` | function (private) | event-mapper.ts |
| `StreamAction.markdown.value` | extended type `string \| MarkdownString` | event-mapper.ts |
| `mapAgentEventToAction` | updated signature (2nd param: `toolVisibility`) | event-mapper.ts |

### Files modified
- `vscode-ext/src/event-mapper.ts`
- `vscode-ext/tests/event-mapper.test.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown HTML rendering | Tool section HTML passes through VS Code's DOMPurify sanitizer. Only allowed tags (`<details>`, `<summary>`) survive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | event-mapper.ts HTML output | accept | All HTML is controlled by the extension (tool name, results from Pi agent). VS Code DOMPurify sanitizer strips dangerous tags. `supportHtml = true` only allows DOMPurify-verified tags. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added. slopcheck: n/a. |
</threat_model>

<verification>
- cd vscode-ext && npx tsc --noEmit passes
- cd vscode-ext && npx vitest run tests/event-mapper.test.ts --reporter verbose passes
- All existing tests still green (backward compatibility verified)
</verification>

<success_criteria>
- event-mapper.ts exports ToolVisibility type and uses it in mapAgentEventToAction
- Tool events buffer per-execution and emit complete HTML blocks
- Quiet mode suppresses all tool output
- Backward compatibility: 1-arg callers (Plan 02-03) still compile and work
- Test coverage for verbose, quiet, and backward-compatible paths
</success_criteria>

<output>
Create `.planning/phases/02-rich-chat-experience/02-02-SUMMARY.md` when done.
</output>
